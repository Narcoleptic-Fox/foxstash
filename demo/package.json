{
  "name": "foxstash-demo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "bash scripts/download-wasm.sh",
    "build:local": "cd .. && wasm-pack build crates/wasm --target web --release && cp -r crates/wasm/pkg demo/pkg",
    "dev": "npx serve ."
  }
}
